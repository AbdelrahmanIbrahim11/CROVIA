fastapi==0.141.1
network_as_code==10.0.0
pydantic==2.13.5
pydantic_settings==2.15.0
python-dotenv==1.2.3
sqlalchemy

# --- added when merging Feature/backend_database ---
# The database code imports these, but they were missing from the branch, so
# the backend could not start.
bcrypt                 # password hashing in usersDB/services.py
email-validator        # required by pydantic EmailStr in usersDB/schemas.py
redis                  # used by app/db/redis.py
psycopg2-binary        # PostgreSQL driver. Swap this if you choose another database.
uvicorn                # the server that runs FastAPI
PyJWT

# --- added when the AI layer and the simulated city were wired in ---
# Both are imported lazily, so the backend still starts without them - it falls
# back to deterministic rules and refuses twin mode. Listing them means a
# deployment that sets GROQ_API_KEY or CROVIA_TWIN actually works rather than
# silently doing less than expected.
numpy                  # the simulated city in sim/twin.py
langchain-groq         # the agent in app/ai/graph.py
langchain-core         # message types the agent uses

